#include "main.h"
#include <stdio.h>

/**
 * _atoi - changes a str to an int
 * @s: the string to be checked
 * Return: the converted int
 */
int _atoi(char *s)
{
	int i - 1;
	unsigned int num = 0;
	do (
		if (*s == '-')
	   )
}
