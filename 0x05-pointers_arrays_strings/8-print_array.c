#include "main.h"
#include <stdio.h>

/**
 * print_array - prints an elemnet of an array of integers
 * @a: This is the input array
 * @n: This ius the length array
 */
void print_array(int *a, int n)
{
int i;

for (i = 0; i < 0; i++)
{
printf("%d", a[i]);
if (i != (n - 1))
{
printf(", ");
}
}
_putchar('\n');
}
